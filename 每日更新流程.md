# 每日更新流程（stock-daily）

> 每個**交易日收盤後**做一次。整條流程：抓資料 → 產生網站檔 → 測試 → 推上線。
> 本文件的指令以 **PowerShell** 為準，路徑是這台電腦實際安裝的位置。

---

## 0. 這台電腦的固定路徑（指令會用到）

| 工具 | 路徑 |
|------|------|
| Python 3.13 | `C:\Users\tongw\AppData\Local\Programs\Python\Python313\python.exe` |
| Node.js | `C:\Program Files\nodejs\node.exe` |
| 系統 Chrome（tpex 抓取用）| `C:\Program Files (x86)\Google\Chrome\Application\chrome.exe` |

> ⚠️ 不要用系統預設的 `python`（那是 Miniconda 3.9，裝不了爬蟲套件）。一律用上面那個 **Python 3.13** 的完整路徑。
> Node 也沒進 PATH，要用完整路徑（或開新終端後 `node` 才會通）。

**追蹤的個股（分點）**：
- 上市（用 `bsr_fetch.py`）：`2344 3037 6257`
- 上櫃（用 `tpex_fetch.py`）：`3105 6261`

> 要增減個股，改下面指令裡的代號即可（上市/上櫃要放對腳本）。

---

## 1. 一鍵設定（每次打開 PowerShell 先貼這段）

```powershell
cd "C:\Users\tongw\Desktop\main\AI_project\stock\stock-daily"
$PY   = "C:\Users\tongw\AppData\Local\Programs\Python\Python313\python.exe"
$NODE = "C:\Program Files\nodejs\node.exe"
$env:PYTHONIOENCODING = "utf-8"
$D    = Get-Date -Format "yyyyMMdd"     # 今天日期；若補跑某天請手動改，例：$D = "20260605"
"今天處理日期：$D"
```

---

## 2. 抓「大盤放量訊號」（量能延續／出關／處置／勝率回測）

```powershell
& $PY volume_system.py --update
```

- 這會：抓最新交易日全市場量價 → 偵測訊號 → 輸出 `out\台股放量訊號_<日期>.xlsx`。
- 量能延續、勝率回測會**自動正確**（因為昨天也有跑，DB 裡有前一天的訊號可接）。
- 跑完看終端摘要，確認「放量訊號 ◯◯◯ 檔」**同時有上市(TWSE)和上櫃(TPEX)**。

> 🟡 **若發現上市訊號異常少 / 幾乎全是上櫃** → 是 TWSE 被限流（靜默失敗）。重跑一次 `& $PY volume_system.py --update` 即可。

---

## 3. 抓「個股分點明細」

**上市**（免視窗，自動解驗證碼）：
```powershell
& $PY bsr_fetch.py 2344 3037 6257 --date $D
```

**上櫃**（會跳出 Chrome 視窗自動過 Turnstile，約 5~12 秒/檔，跑完自動關，別手動關）：
```powershell
& $PY tpex_fetch.py 3105 6261 --date $D
```

- 兩者都輸出 `data\<日期>\<代號>_bsr.csv`。
- 每檔看到 `OK`（Σ買≈Σ賣）才算完整；若 `[!] 買賣不平衡` 或失敗，重跑該檔。

---

## 4. 把大盤 Excel 放進當天資料夾

```powershell
Copy-Item "out\台股放量訊號_$D.xlsx" "data\$D\" -Force
```

此時 `data\$D\` 內應有：`台股放量訊號_$D.xlsx` ＋ 5 個 `*_bsr.csv`。

---

## 5. 產生網站資料

```powershell
& $PY build.py
```

- 看輸出有 `=== <日期> ===` 且 `market.json: {...}`、各檔 `(BSR): {...}`。
- 它會把 `data\` 的 Excel/CSV 轉成 `site\data\` 下的 `.js`，並更新 `index.js`（日期清單）。
- （若改過 build.py 的邏輯才需要全部重建：`& $PY build.py --force`）

---

## 5.5 刷新本週週報（週五~下週四；不滿一週就先呈現部分）

> 週的定義為**週五~下週四**，對齊集保戶股權分散表的每週統計區間。

```powershell
& $PY build_weekly_from_daily.py --current
& $PY build.py
```

- `--current` 會自動抓「最新交易日所在的這一週(以週五為起點)」，把當週**已有的日資料**彙總成週報；不滿一週(1~4 天)就先產部分週，之後每天再跑會自動補到完整（補到下週四）。
- 它只會留**一個**當週桶（換日會自動清掉同週的舊部分桶），不會越積越多。
- 因為它產生的是 `.json`，要再跑一次 `build.py` 把週報包成 `.js` 並更新 `index` 的 `weekly_dates`（所以這裡 build.py 跑第二次）。
- 補某個歷史的週：`& $PY build_weekly_from_daily.py 20260529-0604`（直接指定週鍵，週五~下週四）再 `build.py`。

---

## 6. 煙霧測試（確認前端不會爆）

```powershell
& $NODE site\smoke.js site
```

要看到 `✓ smoke 通過`。若失敗，**先別推上線**，把錯誤貼給我。

---

## 7. 推上線（GitHub Pages 自動部署）

```powershell
git add -A
git commit -m "新增 $D 資料"
git push
```

- 推到 `main` 會觸發 GitHub Actions 自動發佈到 https://tongwade.github.io/stock-daily/ 。
- 確認部署：`gh run list --limit 1`（看到 `Deploy site to GitHub Pages` = success）。
- 約 1~2 分鐘後線上就會更新。

---

## 重點與注意事項

1. **每天順著跑一次就好**：`volume_system.py` 是「逐日累積」的系統，只要每個交易日都跑 `--update`，量能延續鏈、勝率回測追蹤就會自然正確。**不要中斷太多天**（中斷後那幾天的延續鏈會斷，要補的話得逐日 `--backfill` + `detect_continuation`，很麻煩）。
2. **資料庫 `tw_volume.db`**：是核心狀態檔（gitignore，不會上傳）。**千萬別刪**。它目前已含到 2026-06-05。萬一壞了，要重新 `--init`（回溯 7 個月）才能重建，但那樣會缺歷史訊號/勝率（細節見下「疑難排解」）。
3. **時機**：請在**交易日收盤後**跑（盤後資料才齊）。週末/假日跑的話，抓到的是上一個交易日的資料，`$D` 記得手動設成那個交易日。
4. **raw 資料不上傳**：`data\`、`out\`、`*.db`、`*.log` 都被 gitignore，只有 `site\data\` 會發佈。

---

## 疑難排解

| 症狀 | 原因 / 解法 |
|------|------|
| 大盤訊號幾乎全是上櫃、上市很少 | TWSE 被限流靜默失敗 → 重跑 `volume_system.py --update` |
| `大盤漲跌%` 顯示 NaN | market_stats 沒算到 → 重跑 `--update`（正常每天跑不會發生）|
| 量能延續比夥伴少很多 | 前一交易日的延續沒接上（中斷過）→ 對前一日跑 `detect_continuation` 再重跑當日 |
| `tpex_fetch.py` 卡住/沒過 Turnstile | 確認有桌面、Chrome 沒被佔用；重跑該檔 |
| `bsr_fetch.py` 一直失敗 | 可能限流，等幾分鐘重跑；或 BSR 當日尚未產表 |
| smoke 測試失敗 | 先別推，把錯誤訊息貼出來查 |

---

## 最精簡版（懶人 checklist）

```powershell
cd "C:\Users\tongw\Desktop\main\AI_project\stock\stock-daily"
$PY="C:\Users\tongw\AppData\Local\Programs\Python\Python313\python.exe"; $NODE="C:\Program Files\nodejs\node.exe"; $env:PYTHONIOENCODING="utf-8"; $D=Get-Date -Format "yyyyMMdd"
& $PY volume_system.py --update
& $PY bsr_fetch.py 2344 3037 6257 --date $D
& $PY tpex_fetch.py 3105 6261 --date $D
Copy-Item "out\台股放量訊號_$D.xlsx" "data\$D\" -Force
& $PY build.py
& $PY build_weekly_from_daily.py --current   # 刷新本週週報(不滿一週就部分)
& $PY build.py                               # 把週報包成 .js 並更新 index
& $NODE site\smoke.js site
git add -A; git commit -m "新增 $D 資料"; git push
```
